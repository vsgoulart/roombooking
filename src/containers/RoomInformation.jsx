"use strict";

import React, { Component } from "react";
import { connect } from "react-redux";

class RoomInformation extends Component {
  render() {
    return "information";
  }
}

export default connect()(RoomInformation);
